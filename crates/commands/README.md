# Usage
```
scolrs <COMMAND>
```

Try `scolrs help` or `scolrs <COMMAND> --help` to see the help.

# Test
Set env var `TEST_OUTPUT_DIR` to save output of unit tests.

# TODO
- Move some of the unit tests (e.g. tests in svg.rs) to integration tests.